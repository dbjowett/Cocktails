import React from 'react';
import styles from './Profile.module.scss';

function Profile() {
  return <div className={styles.PageContainer}>This is the Profile Page.</div>;
}

export default Profile;
